[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["cd wpage_backend && pip install -r requirements.txt"]

[start]
cmd = "cd wpage_backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
